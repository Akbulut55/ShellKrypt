<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ShellKrypt.Desktop.ViewModels"
             x:Class="ShellKrypt.Desktop.Views.CardsView"
             x:DataType="vm:CardsViewModel">

  <Grid RowDefinitions="Auto,*" Margin="16">

    <!-- Top bar -->
    <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,12">
      <TextBox Watermark="Search cards..."
               Text="{Binding SearchText}"
               Height="38"/>

      <Button Grid.Column="1"
              Content="+  Add New"
              Command="{Binding AddNewCommand}"
              Height="38"
              Margin="12,0,0,0"/>
    </Grid>

    <!-- Card table -->
    <Border Grid.Row="1" CornerRadius="12" BorderThickness="1" Padding="12">
      <Grid RowDefinitions="Auto,*,Auto">

        <!-- Header row -->
        <Grid Grid.Row="0" ColumnDefinitions="70,2*,2*,2*,1*,1*,150" Margin="4,0,4,8">
          <TextBlock Text="ICON" Opacity="0.7"/>
          <TextBlock Grid.Column="1" Text="TITLE" Opacity="0.7"/>
          <TextBlock Grid.Column="2" Text="CARDHOLDER" Opacity="0.7"/>
          <TextBlock Grid.Column="3" Text="NUMBER" Opacity="0.7"/>
          <TextBlock Grid.Column="4" Text="EXP" Opacity="0.7"/>
          <TextBlock Grid.Column="5" Text="CVC" Opacity="0.7"/>
          <TextBlock Grid.Column="6" Text="ACTIONS" Opacity="0.7" HorizontalAlignment="Right"/>
        </Grid>

        <!-- Rows -->
        <ScrollViewer Grid.Row="1">
          <ItemsControl ItemsSource="{Binding Rows}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border CornerRadius="10" Padding="8" Margin="0,4" BorderThickness="1">
                  <Grid ColumnDefinitions="70,2*,2*,2*,1*,1*,150" VerticalAlignment="Center">

                    <!-- Icon circle -->
                    <Border Width="38" Height="38" CornerRadius="19"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            BorderThickness="1">
                      <TextBlock Text="{Binding IconLetter}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 FontWeight="Bold"/>
                    </Border>

                    <!-- Title -->
                    <TextBlock Grid.Column="1" Text="{Binding Title}" IsVisible="{Binding IsViewing}" VerticalAlignment="Center"/>
                    <TextBox  Grid.Column="1" Text="{Binding Title}" IsVisible="{Binding IsEditing}" Height="34"/>

                    <!-- Cardholder -->
                    <TextBlock Grid.Column="2" Text="{Binding Cardholder}" IsVisible="{Binding IsViewing}" VerticalAlignment="Center"/>
                    <TextBox  Grid.Column="2" Text="{Binding Cardholder}" IsVisible="{Binding IsEditing}" Height="34"/>

                    <!-- Number (masked) + eye -->
                    <Grid Grid.Column="3" ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                      <TextBlock Text="{Binding NumberDisplay}" IsVisible="{Binding IsViewing}" VerticalAlignment="Center"/>
                      <TextBox  Text="{Binding Number}" IsVisible="{Binding IsEditing}" Height="34"/>

                      <Button Grid.Column="1"
                              x:CompileBindings="False"
                              Content="👁"
                              Margin="8,0,0,0"
                              Command="{Binding DataContext.ToggleSecretsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}"/>
                    </Grid>

                    <!-- Expiry -->
                    <TextBlock Grid.Column="4" Text="{Binding ExpiryDisplay}" IsVisible="{Binding IsViewing}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="6" IsVisible="{Binding IsEditing}">
                      <TextBox Width="44" Height="34" Watermark="MM" Text="{Binding ExpiryMonth}"/>
                      <TextBlock Text="/" VerticalAlignment="Center"/>
                      <TextBox Width="70" Height="34" Watermark="YYYY" Text="{Binding ExpiryYear}"/>
                    </StackPanel>

                    <!-- CVC -->
                    <TextBlock Grid.Column="5" Text="{Binding CvcDisplay}" IsVisible="{Binding IsViewing}" VerticalAlignment="Center"/>
                    <TextBox  Grid.Column="5" Text="{Binding Cvc}" IsVisible="{Binding IsEditing}" Height="34" Width="70"/>

                    <!-- Actions -->
                    <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                      <Button Content="Edit"
                              x:CompileBindings="False"
                              IsVisible="{Binding IsViewing}"
                              Command="{Binding DataContext.BeginEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}"/>

                      <Button Content="Delete"
                              x:CompileBindings="False"
                              IsVisible="{Binding IsViewing}"
                              Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}"/>

                      <Button Content="✓"
                              x:CompileBindings="False"
                              IsVisible="{Binding IsEditing}"
                              Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}"/>

                      <Button Content="✕"
                              x:CompileBindings="False"
                              IsVisible="{Binding IsEditing}"
                              Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}"/>
                    </StackPanel>

                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <TextBlock Grid.Row="2" Text="{Binding Error}" Foreground="Tomato" Margin="4,10,4,0" TextWrapping="Wrap"/>
      </Grid>
    </Border>

  </Grid>
</UserControl>