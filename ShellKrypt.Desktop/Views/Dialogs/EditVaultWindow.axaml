<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ShellKrypt.Desktop.Views.EditVaultWindow"
        Width="700"
        SizeToContent="Height"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Title="Edit Vault"
        TransparencyLevelHint="None"
        Background="{StaticResource SurfaceRaisedBrush}">
  <Border Background="{StaticResource SurfaceRaisedBrush}"
          CornerRadius="18">
    <Grid RowDefinitions="Auto,Auto">
      <StackPanel Spacing="18"
                  Margin="28">
        <StackPanel Spacing="8">
          <TextBlock Text="{DynamicResource Loc.Dialog.EditVault.Title}"
                     FontSize="30"
                     FontWeight="Black"/>
          <TextBlock Text="{DynamicResource Loc.Dialog.EditVault.Subtitle}"
                     Classes="page-subtitle"
                     TextWrapping="Wrap"/>
        </StackPanel>

        <Grid ColumnDefinitions="*,*"
              ColumnSpacing="16">
          <StackPanel Spacing="8">
            <TextBlock Text="{DynamicResource Loc.Common.DisplayName.Label}"
                       Foreground="{StaticResource TextMutedBrush}"
                       FontSize="10"
                       FontWeight="Black"
                       LetterSpacing="2.2"/>
            <TextBox x:Name="DisplayNameBox"
                     PlaceholderText="{DynamicResource Loc.Common.DisplayName.Placeholder}"/>
          </StackPanel>

          <StackPanel Grid.Column="1"
                      Spacing="8">
            <TextBlock Text="{DynamicResource Loc.Dialog.ImportVault.FileLabel}"
                       Foreground="{StaticResource TextMutedBrush}"
                       FontSize="10"
                       FontWeight="Black"
                       LetterSpacing="2.2"/>
            <Border Background="{StaticResource AppBackgroundSoftBrush}"
                    CornerRadius="10"
                    Padding="14,12">
              <TextBlock x:Name="PathText"
                         Classes="page-subtitle"
                         TextWrapping="Wrap"/>
            </Border>
          </StackPanel>
        </Grid>

        <StackPanel Spacing="8">
          <TextBlock Text="{DynamicResource Loc.Common.Description.Label}"
                     Foreground="{StaticResource TextMutedBrush}"
                     FontSize="10"
                     FontWeight="Black"
                     LetterSpacing="2.2"/>
          <TextBox x:Name="DescriptionBox"
                   PlaceholderText="{DynamicResource Loc.Dialog.EditVault.DescriptionPlaceholder}"
                   AcceptsReturn="True"
                   TextWrapping="Wrap"
                   MinHeight="140"/>
        </StackPanel>
      </StackPanel>

      <Grid Grid.Row="1"
            Background="{StaticResource SurfaceElevatedBrush}"
            ColumnDefinitions="*,Auto,Auto"
            ColumnSpacing="10"
            Margin="28,22">
        <TextBlock Text="{DynamicResource Loc.Dialog.EditVault.Footer}"
                   Classes="page-subtitle"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"/>
        <Button Grid.Column="1"
                Content="{DynamicResource Loc.Common.Cancel}"
                Classes="dialog-cancel-button"
                Click="OnCancelClicked"/>
        <Button Grid.Column="2"
                Content="{DynamicResource Loc.Common.SaveChanges}"
                Classes="dialog-primary-button"
                IsDefault="True"
                Click="OnSaveClicked"/>
      </Grid>
    </Grid>
  </Border>

  <Window.Styles>
    <Style Selector="Button.dialog-cancel-button">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="CornerRadius" Value="10"/>
      <Setter Property="Height" Value="42"/>
      <Setter Property="MinHeight" Value="42"/>
      <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style Selector="Button.dialog-primary-button">
      <Setter Property="Background" Value="{StaticResource AccentGradientBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource AccentForegroundBrush}"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="CornerRadius" Value="10"/>
      <Setter Property="Height" Value="42"/>
      <Setter Property="MinHeight" Value="42"/>
      <Setter Property="FontWeight" Value="Black"/>
    </Style>
  </Window.Styles>
</Window>
