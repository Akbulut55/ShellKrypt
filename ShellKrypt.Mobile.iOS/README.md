# ShellKrypt.Mobile.iOS

Real iOS app head for the shared ShellKrypt mobile shell.

Requirements:

- macOS
- Xcode
- .NET iOS workload
- Apple Developer signing/provisioning

Build/run from macOS with an iOS simulator or device selected.

Before store release, add final icons, launch assets, signing configuration, provisioning profiles, and App Store export compliance answers.
